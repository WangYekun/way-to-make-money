 Spring Cloud Alibaba 大型互联网领域多场景最佳实践
V: ititit111222333

if (ObjectUtils.isEmpty(result)) {
    log.error("ech-system服务调用失败");
    throw new ParamslidateException(UserResultCode.USER_SYSTEM_FEIGN_ERROR);
}
if ( || result.getData() == null) {
    log.error("根据公司id查询公司信息失败！");
    throw new ParamsVadatexception(result.getCode(), result.getMessage());
}
return result.getData();
